spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8888/
      fail-fast: true
    bus:
      refresh:
        enabled: true
      env:
        enabled: true
  kafka:
    bootstrap-servers: http://localhost:9092

